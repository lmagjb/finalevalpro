# Database
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=12345678
DB_NAME=evalpro

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=replace-with-a-long-random-string

# LLM provider for AI recommendation guidance (optional).
# Set ONE of these. If none is set, teachers still see the full
# structured recommendation list — the AI narrative is additive only.
GEMINI_API_KEY=
# GROQ_API_KEY=
# OLLAMA_URL=http://localhost:11434
